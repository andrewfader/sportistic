%h2=@game.title
- if @game.team.photo.small.url
  = link_to image_tag(@game.team.photo.small.url, {title: @game.team.name, alt: @game.team.name}), team_path(@game.team)
- else
  = link_to @game.team.name, team_path(@game.team)
%h3 #{@game.start} to #{@game.end}
.players
  = render "users/list", users: @game.users
= link_to "Play in this game", game_join_path(@game)
