%h3 Recent Players
- User.all.last(5).each do |user|
  - if user.photo.thumb.url.present?
    =link_to image_tag(user.photo.thumb.url, title: user.name, alt: user.name), user_path(user)
  - else
    = link_to user.name, user_path(user)
%h3 Recent Teams
= render "teams/list", teams: Team.all.last(5)
%h3=link_to "Create Team", new_team_path if can? :create, Team
